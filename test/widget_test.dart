// Default Flutter counter test removed. Phase 1 widget tests will live in
// feature-specific files (e.g., test/sign_in_test.dart) and require Firebase
// mocks once those exist.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, equals(2));
  });
}
